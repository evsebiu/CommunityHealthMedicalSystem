spring.application.name=CommunityHealthMedicalSystem

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Seby31may

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Session
spring.session.jdbc.initialize-schema=never

# SpringDoc Configuration - SIMPLIFIED
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Remove debug logging for now

# Force OpenAPI 3.0.1 for compatibility
springdoc.openapi-model-converter=default
springdoc.swagger-ui.operations-sorter=alpha
springdoc.swagger-ui.tags-sorter=alpha
springdoc.api-docs.version=openapi_3_0